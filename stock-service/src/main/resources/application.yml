spring:
  application:
    name: stock-service

server:
  port: 8301

eureka:
  client:
    register-with-eureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8302/eureka/
    instance:
      hostname: localhost


